type BuildingType = {
  id: number;
  name: string;
  imagePath: string;
  street: string;
  postCode: string;
  city: string;
  region: string;
  descirpiton: string;
};

export default BuildingType;
