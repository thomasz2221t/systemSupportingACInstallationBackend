type BuildingType = {
  id: number;
  name: string;
  imagePath: string;
  street: string;
  postCode: string;
  city: string;
  region: string;
  description: string;
  chatId: number;
};

export default BuildingType;
